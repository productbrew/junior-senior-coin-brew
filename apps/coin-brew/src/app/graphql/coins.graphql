query coins($limit: Int!, $skip: Int!) {
  coins(limit: $limit, skip: $skip) {
    id
    name
    price
    currency
    logoUrl
    markets {
      id
      market
      base
      exchange
      quote
    }
  }
}
